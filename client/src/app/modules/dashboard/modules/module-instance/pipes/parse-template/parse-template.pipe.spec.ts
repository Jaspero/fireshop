import { ParseTemplatePipe } from './parse-template.pipe';

describe('ParseTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new ParseTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
